<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.walkvoid.zone.user.business.db.mapper.UserRoleRelMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.github.walkvoid.zone.user.model.entity.UserRoleRel">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <!-- 根据用户ID查询角色ID列表 -->
    <select id="selectRoleIdsByUserId" resultType="java.lang.Long">
        SELECT role_id FROM user_role_rel
        WHERE user_id = #{userId}
    </select>

    <!-- 根据角色ID查询用户ID列表 -->
    <select id="selectUserIdsByRoleId" resultType="java.lang.Long">
        SELECT user_id FROM user_role_rel
        WHERE role_id = #{roleId}
    </select>

    <!-- 批量插入用户角色关联关系 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO user_role_rel (user_id, role_id, create_time, create_by, remark)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.roleId}, #{item.createTime}, #{item.createBy}, #{item.remark})
        </foreach>
    </insert>

    <!-- 根据用户ID删除用户角色关联关系 -->
    <delete id="deleteByUserId">
        DELETE FROM user_role_rel
        WHERE user_id = #{userId}
    </delete>
</mapper>